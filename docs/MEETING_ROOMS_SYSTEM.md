# 📅 نظام حجز قاعات الاجتماعات

## ✨ **نظرة عامة**

تم إضافة نظام شامل ومتطور لحجز قاعات الاجتماعات والاجتماعات الأونلاين إلى بوابة الموظفين. يتيح النظام للموظفين:

- ✅ عرض جميع القاعات المتاحة مع مواصفاتها
- ✅ حجز قاعات الاجتماعات بسهولة
- ✅ دعم الاجتماعات الحضورية والأونلاين والهجين
- ✅ إدارة الحجوزات (عرض، إلغاء)
- ✅ التحقق التلقائي من توفر القاعات
- ✅ إضافة المشاركين ورابط الاجتماع الأونلاين

---

## 🎯 **الميزات الرئيسية**

### 1. **عرض القاعات** (`/portal/rooms`)
- بطاقات تفصيلية لكل قاعة
- إحصائيات: إجمالي القاعات، المتاحة، بها مؤتمرات مرئية، بها بروجكتور
- معلومات القاعة: السعة، الموقع، الطابق، المواصفات
- المميزات: بروجكتور، سبورة، مؤتمرات مرئية، شاشة، صوتيات، WiFi، تكييف

### 2. **إنشاء حجز جديد** (`/portal/bookings/new`)
- اختيار القاعة من قائمة منسدلة
- كتابة عنوان ووصف الاجتماع
- اختيار التاريخ والوقت
- **التحقق التلقائي من التوفر** في الوقت الفعلي
- اختيار نوع الاجتماع:
  - 🏢 **حضوري**: في القاعة
  - 💻 **أونلاين**: عن بُعد (Zoom, Teams, etc.)
  - 🔄 **هجين**: حضوري + أونلاين
- إضافة رابط الاجتماع الأونلاين (للأونلاين والهجين)
- إضافة Meeting ID وكلمة المرور
- إضافة المشاركين
- ملاحظات إضافية

### 3. **عرض حجوزاتي** (`/portal/bookings/my`)
- تصنيف الحجوزات:
  - 🟢 **القادمة**: الحجوزات المستقبلية المؤكدة
  - 🟣 **السابقة**: الحجوزات المكتملة
  - ⚫ **الملغاة**: الحجوزات المرفوضة أو الملغاة
- إحصائيات شاملة
- عرض تفاصيل كل حجز:
  - القاعة والموقع
  - التاريخ والوقت
  - نوع الاجتماع
  - رابط الاجتماع الأونلاين (إن وجد)
  - عدد المشاركين
- إمكانية إلغاء الحجوزات القادمة

### 4. **حالات الحجز**
- 🟡 **قيد المراجعة** (PENDING): تحت المراجعة
- 🟢 **مؤكد** (APPROVED): تم الموافقة
- 🔴 **مرفوض** (REJECTED): تم الرفض
- ⚫ **ملغي** (CANCELLED): ملغي من الموظف
- 🔵 **مكتمل** (COMPLETED): انتهى الاجتماع

---

## 📁 **الملفات المضافة**

### **Backend**
```
src/app/actions/room-bookings.ts
  ├─ getMeetingRooms()           // الحصول على جميع القاعات
  ├─ getMeetingRoomById()        // الحصول على قاعة محددة
  ├─ checkRoomAvailability()     // التحقق من توفر القاعة
  ├─ createRoomBooking()         // إنشاء حجز جديد
  ├─ getMyBookings()              // الحصول على حجوزات الموظف
  ├─ getBookingsByDateRange()    // الحصول على حجوزات في نطاق زمني
  ├─ updateRoomBooking()         // تحديث حجز
  └─ cancelRoomBooking()         // إلغاء حجز
```

### **Frontend - Pages**
```
src/app/portal/rooms/page.tsx                    // صفحة عرض القاعات
src/app/portal/bookings/new/page.tsx            // صفحة إنشاء حجز
src/app/portal/bookings/my/page.tsx             // صفحة حجوزاتي
```

### **Frontend - Components**
```
src/components/bookings/cancel-booking-button.tsx   // زر إلغاء الحجز
```

### **Database**
```
prisma/schema.prisma
  ├─ MeetingRoom model          // نموذج القاعات
  └─ RoomBooking model          // نموذج الحجوزات
```

### **Scripts**
```
scripts/seed-meeting-rooms.js   // بيانات تجريبية للقاعات
```

---

## 🗄️ **قاعدة البيانات**

### **MeetingRoom Model**
```prisma
model MeetingRoom {
  id          String   @id @default(cuid())
  name        String   // اسم القاعة
  nameEn      String?  // الاسم بالإنجليزية
  description String?  // وصف
  location    String   // الموقع
  capacity    Int      // السعة
  floor       String?  // الطابق
  
  // المواصفات
  hasProjector        Boolean
  hasWhiteboard       Boolean
  hasVideoConf        Boolean
  hasScreen           Boolean
  hasSoundSystem      Boolean
  hasWifi             Boolean
  hasAirConditioning  Boolean
  
  // الحالة
  isActive    Boolean
  isAvailable Boolean
  
  imageUrl    String?  // صورة القاعة
  notes       String?  // ملاحظات
  
  bookings    RoomBooking[]
}
```

### **RoomBooking Model**
```prisma
model RoomBooking {
  id          String    @id @default(cuid())
  roomId      String    // القاعة
  employeeId  String    // الموظف
  
  title       String    // عنوان الاجتماع
  description String?   // الوصف
  startTime   DateTime  // وقت البداية
  endTime     DateTime  // وقت النهاية
  
  meetingType String    // PHYSICAL, ONLINE, HYBRID
  
  // للاجتماعات الأونلاين
  onlineMeetingUrl      String?
  onlineMeetingId       String?
  onlineMeetingPassword String?
  
  attendees      String?  // JSON array
  attendeesCount Int
  
  status      String    // PENDING, APPROVED, REJECTED, CANCELLED, COMPLETED
  notes       String?
  adminNotes  String?
  
  sendReminder Boolean
  reminderSent Boolean
}
```

---

## 🚀 **التثبيت والإعداد**

### 1. **تحديث قاعدة البيانات**
```bash
# Generate Prisma Client
npx prisma generate

# Push schema to database
npx prisma db push
```

### 2. **إضافة البيانات التجريبية**
```bash
# إضافة 6 قاعات اجتماعات
node scripts/seed-meeting-rooms.js
```

### 3. **التشغيل**
```bash
npm run dev
```

---

## 📝 **طريقة الاستخدام**

### **للموظف:**

1. **عرض القاعات**
   - اذهب إلى Dashboard → حجز قاعة اجتماعات
   - أو `/portal/rooms`

2. **حجز قاعة**
   - اضغط على "احجز الآن" على القاعة المطلوبة
   - أو `/portal/bookings/new`
   - املأ التفاصيل (العنوان، التاريخ، الوقت)
   - اختر نوع الاجتماع
   - إذا كان أونلاين: أضف الرابط والمعلومات
   - أضف المشاركين (اختياري)
   - اضغط "تأكيد الحجز"

3. **عرض حجوزاتي**
   - اذهب إلى `/portal/bookings/my`
   - شاهد جميع حجوزاتك القادمة والسابقة
   - ألغ الحجوزات القادمة إذا لزم الأمر

---

## 🎨 **التصميم**

### **الألوان المستخدمة:**
- 🔵 **أزرق**: القاعات العامة
- 🟢 **أخضر**: الحجوزات المؤكدة
- 🟣 **بنفسجي**: الحجوزات السابقة
- 🟡 **أصفر**: قيد المراجعة
- 🔴 **أحمر**: مرفوض/ملغي
- 🟠 **برتقالي/كهرماني**: أيقونة حجز القاعات

### **المميزات البصرية:**
- Gradients سلسة
- Hover effects
- Animations
- إشارات التوفر اللحظية
- بطاقات احترافية
- Icons تعبيرية
- RTL Support كامل

---

## 🔜 **التطويرات المستقبلية**

### **المرحلة 2:**
- [ ] تقويم تفاعلي (Calendar View)
- [ ] تذكيرات تلقائية (Email/SMS)
- [ ] تكرار الحجوزات (Recurring bookings)
- [ ] موافقة الأدمن للحجوزات
- [ ] تقارير إحصائية للقاعات

### **المرحلة 3:**
- [ ] دمج مع Microsoft Teams API
- [ ] دمج مع Zoom API
- [ ] QR Code للحجوزات
- [ ] Check-in/Check-out للحجوزات
- [ ] تقييم القاعات بعد الاجتماع

---

## 📞 **الدعم**

للإبلاغ عن مشاكل أو اقتراحات، يرجى فتح Issue في المشروع.

---

**تم التطوير بواسطة**: IT Asset Management System  
**التاريخ**: ديسمبر 2025  
**الإصدار**: 1.0.0
