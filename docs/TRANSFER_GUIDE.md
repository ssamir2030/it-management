# 📦 دليل نقل وتثبيت نظام إدارة الأصول

هذا الدليل يشرح كيفية نقل وتشغيل النظام على جهاز جديد.

---

## 📋 المتطلبات الأساسية

| المتطلب | الإصدار | رابط التحميل |
|---------|---------|--------------|
| Node.js | 18.x أو أحدث | https://nodejs.org |
| PostgreSQL | 15+ | https://www.postgresql.org/download |
| Git (اختياري) | أي إصدار | https://git-scm.com |

---

## 🚀 خطوات التثبيت

### الخطوة 1: نقل ملفات المشروع

انقل مجلد المشروع بالكامل إلى الجهاز الجديد.

**ملفات يجب استبعادها (إن وجدت):**
- `node_modules/` (سيتم تثبيتها لاحقاً)
- `.next/` (سيتم بناؤها لاحقاً)
- `prisma/dev.db` (قاعدة البيانات القديمة)

---

### الخطوة 2: تثبيت PostgreSQL

1. حمّل وثبّت PostgreSQL من الموقع الرسمي
2. أثناء التثبيت، تذكر:
   - اسم المستخدم (عادةً `postgres`)
   - كلمة المرور التي تختارها
   - المنفذ (عادةً `5432`)

---

### الخطوة 3: إنشاء قاعدة البيانات

افتح **pgAdmin** أو **psql** ونفذ:

```sql
CREATE DATABASE "itasset-db";
```

أو من سطر الأوامر:
```bash
psql -U postgres -c "CREATE DATABASE \"itasset-db\";"
```

---

### الخطوة 4: إعداد ملف البيئة

1. انسخ ملف المثال:
```bash
copy .env.example .env
```

2. افتح `.env` وعدّل الإعدادات:

```env
# قاعدة البيانات
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/itasset-db?schema=public"

# المصادقة (غيّر هذه القيم!)
AUTH_SECRET="your-secret-key-here-minimum-32-characters"
NEXTAUTH_SECRET="your-secret-key-here-minimum-32-characters"

# عنوان التطبيق
NEXTAUTH_URL="http://localhost:4002"
NEXT_PUBLIC_APP_URL="http://localhost:4002"
```

**استبدل:**
- `YOUR_PASSWORD` بكلمة مرور PostgreSQL
- `your-secret-key-here` بمفتاح سري طويل (32 حرف على الأقل)

---

### الخطوة 5: تثبيت الحزم

افتح Terminal في مجلد المشروع:

```bash
npm install
```

---

### الخطوة 6: تطبيق قاعدة البيانات

```bash
# تطبيق الـ migrations
npx prisma migrate deploy

# إضافة البيانات الأولية (Admin user, departments, etc.)
npx prisma db seed
```

---

### الخطوة 7: البناء والتشغيل

```bash
# بناء التطبيق
npm run build

# تشغيل في وضع الإنتاج
npm start
```

التطبيق سيعمل على: **http://localhost:4002**

---

## 🔐 بيانات الدخول الافتراضية

| الحقل | القيمة |
|-------|--------|
| البريد | `admin@system.com` |
| كلمة المرور | `admin123` |

> ⚠️ **مهم:** غيّر كلمة المرور فوراً بعد أول تسجيل دخول!

---

## 🌐 للنشر على الإنترنت

### الخيار 1: VPS (DigitalOcean, AWS, etc.)

1. اتبع نفس الخطوات أعلاه
2. استخدم Nginx كـ reverse proxy
3. أضف شهادة SSL باستخدام Let's Encrypt
4. عدّل `.env`:
```env
NEXTAUTH_URL="https://your-domain.com"
NEXT_PUBLIC_APP_URL="https://your-domain.com"
```

### الخيار 2: Vercel + Supabase

1. انشر على Vercel
2. استخدم Supabase أو Neon للـ PostgreSQL
3. أضف متغيرات البيئة في Vercel Dashboard

---

## 🔧 استكشاف الأخطاء

### خطأ: لا يمكن الاتصال بقاعدة البيانات
```
تأكد من:
- PostgreSQL يعمل
- اسم قاعدة البيانات صحيح
- كلمة المرور صحيحة
- المنفذ صحيح (5432)
```

### خطأ: EACCES permission denied
```bash
# على Windows (كـ Administrator)
npm cache clean --force
npm install
```

### خطأ: Prisma Client not found
```bash
npx prisma generate
```

---

## 📁 هيكل الملفات المهمة

```
IT Asset/
├── .env                 # إعدادات البيئة (لا تشاركها!)
├── .env.example         # مثال للإعدادات
├── prisma/
│   └── schema.prisma    # تعريف قاعدة البيانات
├── src/                 # كود التطبيق
├── public/              # الملفات العامة
└── package.json         # الحزم المطلوبة
```

---

## 📞 الدعم

إذا واجهت أي مشاكل، راجع:
- `docs/MILESTONE_CLEAN_BUILD.md` - حلول مشاكل البناء
- `docs/DEPLOYMENT_GUIDE.md` - دليل النشر المتقدم

---

📅 آخر تحديث: 2026-01-14
