# دليل إصلاح مشاكل تسجيل الدخول

## الخطوات المطلوبة لإصلاح تسجيل الدخول

### 1️⃣ التحقق من قاعدة البيانات وكلمة المرور
تم بالفعل! ✅ السكريبت `verify-login.js` أكد أن:
- المستخدم Admin موجود
- كلمة المرور محفوظة بشكل صحيح
- البيانات: admin@system.com / password

### 2️⃣ التحقق من متغيرات البيئة (.env)
يجب أن يحتوي ملف `.env` على:
```env
NEXTAUTH_SECRET="fc85293407923745902384902384092384902384902384"
NEXTAUTH_URL="http://localhost:4002"
```

### 3️⃣ تنظيف الكاش وإعادة التشغيل

**الطريقة الأولى (الموصى بها):**
```powershell
.\start-clean.ps1
```

**الطريقة الثانية (يدوياً):**
```powershell
# حذف مجلد .next
Remove-Item -Recurse -Force .next

# تشغيل السيرفر
npm run dev
```

### 4️⃣ اختبار تسجيل الدخول

1. افتح المتصفح على: `http://localhost:4002/login`
2. أدخل البيانات:
   - **Email:** admin@system.com
   - **Password:** password
3. اضغط "دخول النظام"

### 5️⃣ التحقق من حالة النظام

افتح في المتصفح: `http://localhost:4002/api/debug`

يجب أن ترى:
```json
{
  "status": "ok",
  "env": {
    "NEXTAUTH_SECRET": "Loaded",
    "NEXTAUTH_URL": "http://localhost:4002"
  },
  "db": {
    "connected": true,
    "userCount": 2
  }
}
```

### 6️⃣ مراقبة السجلات (Logs)

عند محاولة تسجيل الدخول، راقب Terminal. يجب أن ترى:
```
🔐 Authenticate action called for: admin@system.com
🔐 Password length: 8
Auth Config - NEXTAUTH_SECRET exists: true
Attempting authorization for: admin@system.com
Password match! Logging in user: <user-id>
✅ signIn successful
```

## 🔍 استكشاف الأخطاء

### إذا ظهرت رسالة "بيانات الدخول غير صحيحة"
1. تأكد من كتابة البريد الإلكتروني بشكل صحيح: `admin@system.com`
2. تأكد من كتابة كلمة المرور بشكل صحيح: `password`
3. شغل السكريبت مرة أخرى:
   ```powershell
   node scripts/verify-login.js
   ```

### إذا ظهرت أخطاء 500
1. تحقق من ملف `.env` وتأكد من وجود `NEXTAUTH_SECRET`
2. احذف مجلد `.next` وأعد التشغيل
3. تحقق من logs في Terminal

### إذا لم يحدث شيء عند الضغط على زر الدخول
1. افتح Developer Console في المتصفح (F12)
2. تحقق من وجود أخطاء JavaScript
3. تحقق من Network tab لرؤية الـ requests

## 📞 الدعم الفني

إذا استمرت المشكلة:
1. التقط Screenshot من رسالة الخطأ
2. انسخ logs من Terminal
3. تحقق من نتيجة `/api/debug`

## ✅ التحديثات التي تمت

1. ✅ إضافة logging تفصيلي في `auth.ts`
2. ✅ إضافة logging في `authenticate` action
3. ✅ إنشاء سكريبت `verify-login.js` للتحقق من كلمة المرور
4. ✅ إنشاء سكريبت `start-clean.ps1` لتنظيف الكاش
5. ✅ التأكد من وجود جميع متغيرات البيئة
