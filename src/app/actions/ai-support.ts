'use server'



// Advanced Keyword Matching (Simulation of AI)
export async function generateAIResponse(message: string): Promise<{ text: string, confident: boolean }> {
    const text = message.toLowerCase()

    // 1. Greetings & General (التحية)
    if (match(text, ['مرحبا', 'هلا', 'السلام', 'hi', 'hello', 'hey', 'صباح الخير', 'مساء الخير'])) {
        return response('مرحباً بك! 👋\nأنا المساعد الذكي لنظام IT Asset.\nكيف يمكنني مساعدتك اليوم؟')
    }

    // 2. Password & Login (كلمة المرور والدخول)
    if (match(text, ['باسورد', 'كلمة المرور', 'رقم سري', 'password', 'login', 'دخول', 'تسجيل دخول'])) {
        return response('في حال نسيت كلمة المرور أو واجهت مشكلة في الدخول:\n1. تأكد من أن زر Caps Lock غير مفعل.\n2. يمكنك إعادة تعيين كلمة المرور من خيار "نسيت كلمة المرور" في صفحة الدخول.\n3. أو تواصل مع المسؤول المباشر لتصفير الحساب.')
    }

    // 3. Printers (الطابعات)
    if (match(text, ['طابعة', 'طباعة', 'printer', 'printing', 'ورق', 'حبر'])) {
        return response('لمشاكل الطابعات:\n1. تأكد من وجود ورق كافٍ في الدرج.\n2. تأكد من تشغيل الطابعة وتوصيلها بالشبكة.\n3. إذا كانت الطابعة "Offline"، حاول إعادة تشغيلها.\nلطلب حبر جديد، استخدم نموذج "طلب مستهلكات" من القائمة.')
    }

    // 4. Internet & Network (الإنترنت والشبكة)
    if (match(text, ['نت', 'انترنت', 'شبكة', 'واي فاي', 'wifi', 'internet', 'network', 'بطيء', 'slow'])) {
        return response('إذا كان الإنترنت بطيئاً أو مقطوعاً:\n1. تأكد من توصيل كابل الشبكة (Ethernet) جيداً.\n2. جرب إيقاف وتشغيل الواي فاي في جهازك.\n3. إذا كانت المشكلة عامة، فقد يكون هناك صيانة في الشبكة.\nيمكنك رفع تذكرة "مشكلة تقنية" للفحص.')
    }

    // 5. Email & Outlook (البريد الإلكتروني)
    if (match(text, ['ايميل', 'بريد', 'email', 'outlook', 'mail', 'رسائل'])) {
        return response('لمشاكل البريد الإلكتروني:\n1. تأكد من امتلاء صندوق الوارد (Mailbox Full).\n2. جرب الدخول من المتصفح (Webmail) للتأكد من الخدمة.\n3. إذا كنت لا تتلقى رسائل، تأكد من إعدادات الـ Spam.')
    }

    // 6. Computer Performance (الجهاز بطيء)
    if (match(text, ['جهاز', 'لابتوب', 'كمبيوتر', 'pc', 'laptop', 'بطئ', 'ثقيل', 'slow', 'performance'])) {
        return response('إذا كان جهازك يعمل ببطء:\n1. أغلق البرامج غير المستخدمة.\n2. أعد تشغيل الجهاز (Restart) - هذا يحل 80% من المشاكل!\n3. تأكد من وجود مساحة كافية في القرص C.\nإذا استمر البطء، قد نحتاج لترقية الرامات أو الهارد.')
    }

    // 7. Software & Applications (البرامج)
    if (match(text, ['برنامج', 'تطبيق', 'program', 'app', 'office', 'excel', 'word', 'تثبيت', 'install'])) {
        return response('لتثبيت برامج جديدة أو تحديثها:\nيمكنك تقديم "طلب برامج" من خلال بوابة الخدمات.\nتأكد من تحديد اسم البرنامج والنسخة المطلوبة.')
    }

    // 8. VPN & Remote Access (الدخول عن بعد)
    if (match(text, ['vpn', 'remote', 'عن بعد', 'اتصال خارجي'])) {
        return response('لخدمة الـ VPN:\nيجب أن يكون لديك حساب مصرح به.\nتأكد من تثبيت عميل FortiClient أو Cisco AnyConnect حسب التوجيهات.\nلطلب الصلاحية، الرجاء استخدام نموذج "طلب صلاحيات".')
    }

    // 9. ERP & Systems (الأنظمة الداخلية)
    if (match(text, ['erp', 'نظام', 'سيستم', 'دخول النظام', 'system'])) {
        return response('لمشاكل الأنظمة الداخلية (ERP):\nيرجى تحديد رسالة الخطأ التي تظهر لك بدقة ورفع تذكرة دعم فني مع إرفاق صورة للمشكلة (Screenshot) لتسريع الحل.')
    }

    // Default Fallback
    return {
        text: 'عذراً، لم أفهم استفسارك تماماً. 🤖\n\nأنا نظام ذكي (تحت التدريب) ولست بشراً.\nيمكنك صياغة السؤال بكلمات مفتاحية بسيطة (مثل: طابعة، انترنت، باسورد).\n\nأو اضغط على زر "تحويل لموظف دعم" بالأعلى للتحدث مع زميل من فريق الدعم المباشر.',
        confident: false
    }
}

// Helper to match substrings
function match(text: string, keywords: string[]): boolean {
    return keywords.some(k => text.includes(k))
}

// Helper to return format
function response(text: string): { text: string, confident: boolean } {
    return { text, confident: true }
}
